void main() {
  // > Verifica se o valor da esquerda é maior que o da direita
  // >= Verifica se o valor da esquerda é maior ou igual
  // < Verifica se o valor a esquerda é menor
  // <= Verifica se o valor a esquerda é menor igual
  // == verifica se o valor é igual
  // != Verifica se o valor é diferente

  // Expressão Relacional
  print(1 > 2);

  var idade = 14;
  print(idade > 18);

  print('Black Friday');
  final macbook = 5000;

  print('Vou comprar o macbook?');
  print(macbook < 9000);
}
